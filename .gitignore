__pycache__/
*.pyc

# Save files (runtime game state, not source)
*.json

# Atlas's own tooling output for this project (bugs/decisions/lessons/
# repair history) - not game source, kept out of this repo
memory/
.atlas/
.atlasignore

# Local balance-test output, not source
balance_sweep_out/

# Human play-session transcripts (src/playlog.py output) - runtime, not source
apocrysis_playlog_*.txt
.dev_playtest_profile.json
