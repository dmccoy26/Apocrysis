# Phase A todo list — Apocrysis v5 (world-investigation spine)

Source list for `atlas todo add --file docs/PHASE_A_TODO.md
--continuity-key apocrysis-v5-phase-a`. Ordered; earlier items gate
later ones. Each is routed to Atlas first (see
`ATLAS_CAPABILITY_LOG.md`), hand-written only if Atlas fails twice.

Locked decisions this builds on: `PHASE_A_DECISIONS.md` (truth A "The
Cordon", `World` seam now, fact-DAG only). Plan: `APOCRYSIS_ROADMAP.md`
§9 Phase A.

- **Vocabulary rename, part 1 — CAMPAIGN_LENGTH constant:** In src/constants.py and every reference, rename the `CAMPAIGN_LENGTH` constant to `EXPEDITIONS_PER_CAMPAIGN` and add a sibling `EXPEDITIONS_PER_CHAPTER` list/constant is NOT wanted yet — only the rename. Update src/campaign.py, src/game.py, src/cli.py, src/tui.py, src/playlog.py, src/mixins/world_mixin.py, src/mixins/ui_mixin.py, src/mixins/persistence_mixin.py and the two test files. No behaviour change. Both test suites must stay green.
- **Vocabulary rename, part 2 — expeditions_completed:** Rename the `expeditions_completed` parameter/attribute/dict-key to `expeditions_completed` staying as-is is fine at the storage layer, but the player-facing framing function `campaign.chapter_intro()` and its callers should speak in terms of Campaign / Chapter / Expedition. Introduce `campaign.chapter_for_expedition(n)` returning a 1-based chapter index from a `_CHAPTER_BOUNDS` table (CH1 maps 1-5, CH2 6-9, CH3 10-14, CH4 15-19, CH5 20-24, FIN 25) and have `chapter_intro` use it. Keep the existing `_CHAPTERS` prose. No save-format change.
- **worlds/ package + World dataclass (the seam):** Create src/worlds/__init__.py and src/worlds/base.py defining a `World` dataclass (or Protocol) with fields: `key: str`, `title: str`, `encounter_table`, `tile_vocab: dict`, `prose_voice: dict`, `world_facts` (list, may be empty for now), `ending` (callable or None). Add `src/worlds/silence/__init__.py` exporting `SILENCE = World(key="silence", title="The Apocrysis", ...)` populated from the values currently hard-coded in the engine (encounter table, tile names). Nothing consumes it yet — this item is just the module + a passing import test in src/tests/test_worlds.py.
- **Engine takes a World:** Thread the `World` object into `Game.__init__` (src/game.py) as `self.world`, defaulting to `worlds.silence.SILENCE`. Replace the hard-coded encounter table and tile-name lookups in src/mixins/world_mixin.py and src/zombies.py with reads from `self.world`. Behaviour identical for world "silence"; add a test that a second dummy World with a different tile_vocab changes the rendered tile name.
- **WorldFact dataclass + The Cordon DAG (CH1+CH2):** Create src/worlds/silence/truth.py with a `WorldFact` dataclass (`id`, `thread` in {disappearance,dead,response}, `chapter:int`, `milestone:bool`, `statement:str`, `needs:list[str]`) and `WORLD_FACTS`: ~10 facts covering CH1 (the Silence — why is everyone gone) and CH2 (the Infected — where it started, why early/late differ), ~3 flagged `milestone=True` (M1 evacuation was organised, M2 regional not nationwide, M4 infected existed before the evacuation). `needs` must form a DAG with CH1 facts having no cross-chapter needs. Add src/tests/test_world_truth.py asserting the DAG is acyclic and every `needs` id resolves.
- **DiscoveryTemplate + build_mystery target_fact path:** In src/escape.py add a `DiscoveryTemplate` dataclass binding one `WorldFact.id` to an existing MECHANISMS family key + role-label overrides + evidence-text templates. Give `build_mystery(game)` an optional `target_fact` path: when set, it picks a template for that fact instead of the random mechanism roll, and stamps `mystery.world_fact_id`. Random path unchanged when `target_fact` is None. Extend test_escape.py: a mystery built with `target_fact` carries the id and is still valid/reachable.
- **World Investigation persistent state:** Add a `WorldInvestigation` structure (per-fact status KNOWN/SUSPECTED/UNKNOWN, per-thread % derived) following the existing class-var + profile round-trip pattern used for `_recent_signatures` in src/mixins/persistence_mixin.py (save_profile ~line 423, load_profile). It persists across death/new-survivor; player level and gear do not. Next-expedition target = the first UNKNOWN fact whose `needs` are all KNOWN. Test: solving a mystery with a `world_fact_id` flips that fact to KNOWN and it survives a save/load round-trip.
- **World Investigation screen:** Add a top-level view (key `w` from the `>` prompt and shown between expeditions) rendering the §8 layout from APOCRYSIS_ROADMAP.md — per-thread progress bars, KNOWN facts as `✓`, UNKNOWN as `?`, no schema vocabulary leaked (never show `thread: response`). src/mixins/ui_mixin.py + a classic-mode text version. Snapshot-style test on the rendered string.
- **campaign.py chapter intros keyed to investigation progress:** `chapter_intro` chooses its line from the World Investigation state (how many milestones KNOWN) rather than raw expedition count, so replaying an early map after deaths doesn't repeat "Expedition 1 of 25". Falls back to expedition count when investigation state is empty.
- **Milestone discovery banner:** When a solved mystery flips a `milestone=True` fact to KNOWN, fire a distinct `announce_event` banner (kind `milestone`) — the "wait, they didn't all die" beat — separate from the ordinary NEW DISCOVERY banner. Wire through tui.py announce_event and the classic-mode text path. Test that crossing a milestone emits exactly one milestone banner.
